[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rippleguard-security"
version = "0.1.0"
description = "Security analysis tools for RippleGuard"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]