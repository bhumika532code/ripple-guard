rules:
  - id: rippleguard-command-injection
    languages:
      - python
    message: User-controlled data reaches a command execution sink.
    severity: ERROR
    mode: taint

    pattern-sources:
      - pattern: input(...)

    pattern-sinks:
      - pattern: os.system(...)
      - pattern: os.popen(...)